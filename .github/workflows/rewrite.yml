name: Editorial Rewrite Pipeline

on:
  workflow_dispatch:
    inputs:
      chapter:
        description: "Chapter number to process (leave empty for all chapters)"
        required: false
        type: string
        default: ""
      mode:
        description: "Pipeline mode"
        required: true
        type: choice
        options:
          - full
          - bibles-only
          - assemble-only
        default: "full"

jobs:
  rewrite:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Configure git
        run: |
          git config user.name "charlotte-pipeline"
          git config user.email "charlotte@pipeline.local"

      - name: Run editorial rewrite pipeline
        env:
          NIM_API_KEY: ${{ secrets.NIM_API_KEY }}
        run: |
          ARGS=""

          # Add mode flag
          if [ "${{ inputs.mode }}" = "bibles-only" ]; then
            ARGS="$ARGS --bibles-only"
          elif [ "${{ inputs.mode }}" = "assemble-only" ]; then
            ARGS="$ARGS --assemble-only"
          fi

          # Add chapter flag if provided
          if [ -n "${{ inputs.chapter }}" ]; then
            ARGS="$ARGS --chapter ${{ inputs.chapter }}"
          fi

          python orchestrator/rewrite_conveyor.py $ARGS

      - name: Final push
        if: always()
        run: |
          git add output/ || true
          if ! git diff --cached --quiet; then
            git commit -m "chore(rewrite): final output [mode=${{ inputs.mode }}, chapter=${{ inputs.chapter || 'all' }}]"
          fi
          git push || true
