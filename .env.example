# Charlotte local environment
# Copy to .env locally if your runner loads dotenv, or export these variables in your shell.

# Used when provider.type is nim or openai_compatible.
CHARLOTTE_API_KEY=replace_me

# Example only. Actual NIM values will be configured in config.yaml when available.
# CHARLOTTE_NIM_BASE_URL=https://your-nim-endpoint/v1
# CHARLOTTE_NIM_MODEL=your-model-name
